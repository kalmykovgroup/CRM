<UserControl x:Class="KTSF.Components.Window.SignInPageComponent.SignInWinUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KTSF.Components.Window.SignInPageComponent"  
             d:DataContext="{d:DesignInstance Type=local:SignInWinComponent}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
 
    <Grid>
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Background" Value="{StaticResource ColorGreen}"/>

            </Style>
        </Grid.Style>


        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="300">
            <TextBlock FontSize="40" HorizontalAlignment="Center" FontWeight="DemiBold" Margin="0 0 0 50">
                <Run Text="CRM" Foreground="White"/>
                <Run Text="KTSF" Foreground="{StaticResource ColorCompanyNameYellow}"/>
            </TextBlock>

            <ContentControl Content="{Binding FormContainer.UserControl}"/> 

        </StackPanel>

        <ContentControl Foreground="White" Template="{StaticResource ContentControlLoad}"/>

    </Grid>
</UserControl>
